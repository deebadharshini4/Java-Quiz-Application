# Java-Quiz-Application
A Java console-based quiz application that displays multiple-choice questions, accepts user input, verifies answers using a predefined answer key, and calculates the final score. It uses arrays to store questions, options, and answers, and loops to control flow and present each question sequentially.
